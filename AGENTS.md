<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- bmad:context -->
<!-- Verified 2026-08-19 against 6d4c784289c1341d3e89fbf0a26536ce475fea1f. Managed by bmad-project-context; edits inside this block are replaced on refresh. Keep anything you want preserved outside the markers. -->

## anki-english-app

Single-flow app (no auth, no database) that turns an Anki deck into an AI-generated dialogue, then a real-time spoken-practice session. All persistence is `localStorage` — there is no backend datastore. Flow: `/` (deck picker) → `/deck/[deckName]` (dialogue generation) → `/session?id=...` (turn-by-turn practice, Epic 2) or `/practice?id=...` (the legacy Gemini Live screen, kept working until Story 2.8 removes it).

## Where things are

- Vocabulary loading: `lib/vocabulary/` — `types.ts` holds the neutral, transport-free `VocabularyItem` / `VocabularyCollection` types **and** the `VocabularySource` contract (import these, never source-specific ones); `source.ts` holds only the registry (`getVocabularySource()`, defaults to Anki) and re-exports the interface; `sources/anki.ts` is the one concrete implementation; `setup-hint.ts` parses a source's connection-help steps (`**bold**`, `` `code` ``) into React-free segments. Anki internals (`AnkiCard`, HTTP calls, field parsing) are module-private — add a new source as a sibling under `sources/`, don't widen the Anki module's exports. Extraction in `sources/anki.ts` is content-driven, not note-type-driven: clean the `Front` (strip `[sound:…]`, `[…]` IPA groups, trailing POS markers, edge quotes/ellipsis/terminal punctuation) and use it if it is usable English — non-empty, ≤ 80 chars, no interior ellipsis/`__` blank, no Vietnamese-specific letter; otherwise fall back to the first *usable* `<b>`/`<strong>` in `Back` (BBC-Reactor shape), meaning = rest of Back else stripped Front; otherwise drop the card. Items are deduped by word, case-insensitively. Its `VIETNAMESE_LETTERS` is deliberately *narrower* than the validator's (plain French-style accents excluded so `café` survives) — keep the two copies independent. Concrete sources import the interface from `types.ts`, never from `source.ts`, so the registry stays a leaf.
- Anki access goes through `app/api/anki/route.ts` — never call `http://127.0.0.1:8765` (AnkiConnect) directly from client code, it has no CORS headers.
- Dialogue script: `lib/dialogue/` — `types.ts` holds the transport-free `DialogueScript` (`version: 3`, a list of turns each with `speaker: "system" | "learner"`, `text`, its own `targetWords`, and — on `learner` turns only — `hints`) plus the shared constraint constants (`MIN_TURNS`, `MAX_TARGET_WORDS_PER_TURN`, `MAX_EXTRA_HINT_KEYWORDS`, `MIN_LEAKED_CONTENT_WORDS`, …); `validate.ts` holds `validateScript(script, requestedWords)`, the **single definition of a valid script**, and the `isDialogueScript` shape guard. Change a rule there, not in the prompt — and when a rule has a number, interpolate the constant into the prompt so the model is told the rule that is actually enforced.
- `words.ts` owns whole-word matching and safe rendering: `containsWord` and `renderHighlightedHtml(text, words)`. Target words are carried as data — never recover them by substring search (`cold` matches inside `colder`). Every `dangerouslySetInnerHTML` site fed model or user text must go through `renderHighlightedHtml` (or `escapeHtml`): it highlights on raw text and escapes per segment, so escaping can neither break a match nor be broken by one. Don't reintroduce `highlightWords(escapeHtml(x))` — that order corrupts entities. `lib/gemini.ts`'s older `highlightWords` survives only inside `DialogueDisplay`'s legacy branch.
- Hint ladder (`DialogueHints`): two rungs in fixed order — `situation` (level 1, **Vietnamese**: when the line is used, never a translation of it) and `keywords` (level 2: the turn's own target words plus at most `MAX_EXTRA_HINT_KEYWORDS` other content words, every one of them a word from that line). There is deliberately no third rung and no level may reveal the whole line. Hints are generated in the **same** Gemini call as the script and stored with it — opening one must never cost a network request, so never add a hint-fetching endpoint. Only `learner` turns carry them, and the route strips stray ones before responding. `hasHints(script)` / `turnHasHints(turn)` report a ladder a learner could actually open — presence alone is not enough, since the accept-with-faulty-hints path can ship an empty one. `stopwords.ts` (English function words, `contentWords`, `containsSequence`) is what makes "at most 2 other content words" and "borrows ≥`MIN_LEAKED_CONTENT_WORDS` of the line's content words in a row" checkable; it is a *sequence* view of text and is not a second word-boundary matcher — `words.ts` still owns target-word matching. Pass the turn's target words as `alwaysContent` wherever it is called: the list contains words an A2/B1 deck teaches as vocabulary (`like`, `right`, `well`), and a target word must never be treated as glue.
- Script versions: 1 = the pre-Story-1.2 markdown string (`HistoryEntry.legacyDialogue`, never a `DialogueScript`), 2 = structured, no hints, 3 = with hints. `isDialogueScript` accepts **every** version in `SUPPORTED_SCRIPT_VERSIONS` so older stored entries keep loading; `validateScript` insists on the current one, because only fresh model output has to be current. When bumping the version, extend the array in the same commit — `types.test.ts` pins that `DIALOGUE_SCRIPT_VERSION` is a member, and breaking it makes every newly saved entry unreadable. A v2 entry is read as-is and **never upgraded in place**: nothing rewrites stored scripts, so v2 entries simply have no hints forever. Consumers must treat `hints` as optional.
- Dialogue generation: `lib/gemini.ts` + `app/api/dialogue/route.ts`. The route asks Gemini for JSON against a `responseSchema`, assigns each turn's `index` from array position, validates, and on a violation retries **exactly once** on the same model with the violations fed back, then returns 422. Model fallback is for transport failures only — a validation failure means the model understood and got it wrong. Only a *validated* rejection may produce a 422; a transport failure on the repair call is a 500. `validateScript` reports its findings in **three separate arrays**: fatal `violations`, `hintViolations`, and non-fatal `softViolations` (today only the consonant-clash rule). All three are fed to the one repair attempt, but only fatal `violations` can cost the user a generation — hint and soft faults never can. Concretely, both attempts stay on the table and the route returns the **better of the two**: any fatal-clean attempt beats none (fewer hint+soft faults wins, ties keep the earlier), and a 422 is only possible when *neither* attempt was fatal-clean; its `violations` body is fatal-only. So a repair that transport-fails, or that regresses and breaks a fatal rule, cannot throw away a first attempt whose script was already valid. Keep the three arrays apart; don't merge them, and don't reduce the decision to "look at the last attempt". The `violations` array in a 422 body is diagnostic — English, model-facing — and must never be shown to the user; the Vietnamese `error` string is the user-facing text.
- Voice practice: `hooks/useGeminiLive.ts` + `public/audio-processor.worklet.js`.
- Native sample audio: `app/api/tts/route.ts` (Azure TTS **proxy**) + `lib/sample-audio.ts` (decisions) + `hooks/useSampleAudio.ts` (transport and React state) + a play control per turn in `DialogueDisplay.tsx`. Every line of **both** roles gets audio, generated **once** and kept: `SPEAKER_VOICES` in `lib/dialogue/types.ts` gives the two roles two *distinct* en-US neural voices (one voice reading both parts removes the only cue that separates them by ear), and the blob is keyed by `sha256(voice + " " + text)` — content, not `HistoryEntry.id`, because audio is generated before the user clicks save and there is no id yet. Content keys make duplicate lines share one blob for free and keep the same line on the other role separate. Generation runs in the **background** (the script must be readable immediately) and **sequentially** (the free Azure F0 tier is rate-limited) — don't parallelise it, and note that a module-level in-flight map in `sample-audio.ts` shares one synthesis between concurrent callers so a React StrictMode double-mount doesn't buy the first line twice. It is driven **solely** from `DialogueDisplay`, and nothing resumes it: navigating away mid-run leaves the remaining lines unsynthesised until that script is shown again. Three failure modes, deliberately different — a per-line fetch failure (timeout, 502) marks that turn `failed` and the loop continues, because one bad line must not cost the other eleven; a *provider* failure (`/api/tts` 503 or 401 — no credential or a rejected one) and a *write* failure both stop the loop and produce **one** Vietnamese notice, because both are true of every remaining line. Whenever the loop stops early, the turns it never reached are marked `failed` rather than left `idle` — `idle` means "still queued" and a control stuck there forever promises audio that is not coming. None of it ever costs the user the script.
- Turn-by-turn practice: `lib/session.ts` (decisions) + `components/SessionView.tsx` (screen) + `app/session/page.tsx` (route `/session?id=…`). Same split as Story 1.4 and for the same reason — `vitest` has no jsdom — so **everything worth a test lives in `lib/session.ts`**: session status (`idle | running | finished`), the turn cursor, per-completed-turn stats, and `turnPlan(turn, status)`, which says what advances a turn. The claim the module exists to pin is that **no projection ever carries a learner turn's text**: the screen renders only `TurnView`, whose `text` is `null` and whose `targetWords` is empty on a `learner` turn (a target word is a literal substring of the line, so handing it over is a partial reveal). The one sanctioned reveal is Story 2.5's third-failure path and will need its own projection — don't add a flag to this one. **The learner's line must not leak through the speaker either** — Story 1.4 synthesises *both* roles, so a blob for their line exists locally. `audioTurnToPlay(script, state, status)` is the only thing that decides which turn may sound and returns `null` for every learner turn; `SessionView` never picks an index itself. Advance rules: a `learner` turn has `fallbackMs: null` and is never ended by a timer — Story 2.2 made "Tiếp" the exit, and `advanceDelayMs` also returns `null` while `isRecording`; a `system` turn always has a finite `fallbackMs`, so a `failed`, queued or autoplay-blocked line still shows its text and still advances — audio enriches a turn, it never gates one. The arithmetic is `advanceDelayMs(...)`, where `null` means *schedule nothing* (a learner turn, a line still sounding, or a live capture); reading it as `0` makes the learner's turn flash past. Playback must be **stopped** on every turn change, on `endSession` and before any restart (`shouldStopPlayback`, which takes a `PlaybackContext`: `phase: "recording"` silences everything, `replayTurn` protects a deliberate replay of a turn that is no longer current). Use `useSampleAudio.stop()` for that, **never** `play(playingTurn)` — `play` is a toggle that only works once `playingTurn` is set, and it is set only after an async IndexedDB read, so a line tapped a beat earlier is still `null` and the toggle no-ops while the audio starts a tick later. `stop()` bumps the play token, so a read still in flight is superseded. Every advance and every stop goes through a `setTimeout`, never a synchronous `setState` in an effect body — that is the lint error this screen must not add a fourth of. The script handed to `useSampleAudio` must be **referentially stable** (the hook keys all its state on object identity), which is why `app/session/page.tsx` derives the entry in a `useMemo` during render rather than an effect, gated on a `useSyncExternalStore` hydration flag so `next dev`'s on-demand SSR (where `localStorage` is empty) doesn't mismatch the client. Nothing is persisted: in-memory stats survive "Kết thúc buổi" but not a reload — Story 2.3 brings the first metrics worth a storage layer. The screen owns the viewport: `body:has(.session-screen)` pins the document to `100dvh` because the global `body { min-height: 100vh }` would otherwise let the whole page scroll by the mobile toolbar's height and take the bottom control row with it, and `app/layout.tsx` exports `viewport` with `viewportFit: "cover"` so `env(safe-area-inset-*)` resolves to more than `0`. Deliberately **absent**: attempt limits, latency metrics (Stories 2.5–2.6) — scoring and the hint ladder are Stories 2.3 and 2.4. The mic's `disabled` state is the *only* turn indicator — don't add a "your turn" badge. The `?id=` lookup is `findEntry` in the same module (there is no `getById` in `lib/history.ts`), so "no `id`" and "unknown `id`" collapse to one tested state.
- Recording a turn (Story 2.2): `lib/wav.ts` (audio maths) + `lib/recording.ts` (keys, notices, storage decisions) + `hooks/useTurnRecorder.ts` (browser wrapper) + the mic and the two replay controls in `SessionView.tsx`. Capture is **PCM 16 kHz / 16-bit mono in WAV** through the shared `public/audio-processor.worklet.js` — `MediaRecorder` is not an option, it yields webm/opus — and the mic is **tap to start, tap again to stop**, never press-and-hold. `lib/wav.ts` is the pure audio-maths module and everything in it is a function over `Int16Array[]`: `encodeWav`, `peakLevel`, `durationMs`, `trimSilence` (leading/trailing silence only — interior pauses are part of the take, and an **empty result means "nothing was said"**, which is a notice and not a stored file), plus the low-peak policy (`LOW_PEAK_THRESHOLD` + `isPeakTooLow`) that `app/lab/pronunciation/page.tsx` also reads now. Keep it pure and node-testable; don't move audio maths into the hook. **The mic no longer advances the turn** — `micAction(script, state)` says whether the button records, stops or is inert, `canContinue` says when "Tiếp" appears, and "Tiếp" lives in the turn bubble so the control row keeps its three fixed slots (hint · timer · mic). `completeCurrentTurn` refuses to fire mid-recording and `advanceDelayMs` returns `null` while `isRecording`, so no timer can cut a take short. **The reveal rule narrows rather than breaks**: the learner's line is still never rendered as text in any state, but the *native reading* of it unlocks once a take exists — that is `sampleReplayUnlocked`, a **second, distinct projection**, deliberately separate from `audioTurnToPlay`, which still returns `null` for every learner turn unconditionally so the existing leak tests keep meaning what they say. Replay is unlimited and local: no `/api/tts`, no `/api/pronunciation`, no quota spend. That rule holds on **two** channels, not one: `shouldStopPlayback`'s `phase: "recording"` covers the native-sample channel, and `takeReplayUnlocked` returns `false` mid-capture because take playback runs on its own `<audio>` element in `useTurnRecorder` and consults nothing else — gate the *control* on the predicate, don't read `view.take` directly. A capture failure is classified into four (`permission-denied` / `no-device` / `device-busy` / `unavailable`) and said **once**, but only `isPermanentCaptureFailure` ones (a denial, no device) set `micBlocked` and make the mic inert — a busy device or a one-off worklet fetch stays retryable. The escape hatch is a **separate** flag, `takeBlocked`: set by a blocked mic, a failed write, a full disk, or a capture graph that delivered no signal, it opens "Tiếp" *without* touching the mic. Without it a full quota dead-ends the session — every retry fails identically while the notice promises "buổi luyện vẫn đi tiếp bình thường". `stop()` distinguishes **no samples at all** (a suspended `AudioContext`, which iOS and Safari hand back — hence `ctx.resume()`) from a take trimmed to nothing; answering the first with "nói to hơn" is advice that cannot work. `autoGainControl` is deliberately **off** (it normalises exactly what `isPeakTooLow` measures) and `channelCount` is `{ ideal: 1 }` (an exact value raises `OverconstrainedError` on stereo-only hardware, which would blame the user's microphone for our constraint). The low-peak warning is a **signal diagnostic only** — never an excuse offered for a result. Notices never say "tải lại trang" mid-session: the session is in memory and a reload discards every completed turn.
- Scoring a turn (Story 2.3): `lib/pronunciation.ts` (all the judgement) + `hooks/useTurnScorer.ts` (transport) + the score slot in `lib/session.ts` + the card in `SessionView.tsx` + `.session-score*` in `globals.css`. **The pass gate is phoneme-level, on the turn's own target words, and nothing else**: every phoneme of every target word ≥ `PASS_THRESHOLD` (30) ⇒ Đạt; below 30 blocks; 30–59 is a `!` warning that never blocks (`WARN_THRESHOLD` = 60). `PronScore`, word-level `AccuracyScore`, `ErrorType`, `FluencyScore`, `CompletenessScore` and `ProsodyScore` may be *displayed* (the lab does) but **may never gate anything** — measured on this project, `walked` scored 97 at word level with its final `/t/` at 0 and `ErrorType: "None"`, and careless reading beat careful reading on `PronScore` in 2 of 3 sentences because care costs fluency and prosody. Any gate above the phoneme rewards swallowing sounds. A target word Azure never reported fails **while other words did score** — that response was judgeable and the word was not said. `passed: null` is the *unjudgeable* answer and there are exactly two ways to reach it, neither of them "the learner did badly": the turn carries no target words at all (a gate over an empty set evaluates to a confident `✓` on nothing), or the response carries **no phoneme score anywhere** — not merely none on a target word — which is what a silent `Dimension` downgrade to Basic looks like, since it strips exactly those fields and answers 200 and `parseAssessment` drops an absent `AccuracyScore` rather than defaulting it to 0. Both are reported as a scoring failure, never guessed either way. Three tiers: 1 = target word (decides Đạt), 2 = a structural pattern on a **non-function** word (`ending` / `final-consonant` / `cluster`, from `tier2Pattern`, decided structurally without looking at the score), 3 = function words and unremarkable misses, behind `<details>`. Tier 1 beats tier 2 beats nothing; a *function* word stays tier 3 even with a pattern, or every weak `/d/` in every `and` buries the two findings that matter. **`scoreCard` is the fourth projection** (after `TurnView`, `audioTurnToPlay`, `sampleReplayUnlocked`) and a separate one for the same reason 2.2's was: it must name target words to be useful, so `TurnView` keeps `text: null` / `targetWords: []` and the existing leak tests keep meaning what they say. It is **handed the line and never returns it** — what it may name is the target words plus badly-scored words, bounded by `MIN_LEAKED_CONTENT_WORDS` (the hint validator's own rule) so the chips can never add up to a reconstructable run; `namedWords(card)` exists so that is testable from outside. The bound holds because every word is **claimed** before it is printed (`createNamer`), the target words included. "`MAX_TARGET_WORDS_PER_TURN` (2) < `MIN_LEAKED_CONTENT_WORDS` (4), so the targets are safe by construction" is true only for *single-word* targets, and an Anki unit is a phrase as often as a word (`lib/dialogue/words.ts` names `I don't mean to interrupt` as a real one) — two phrases are more than four tokens of the line in a row. A phrase that cannot be printed whole is printed by **one** token of itself (its worst-scoring one) or not at all, and `namedWords` walks **every string the card carries**, `detail` and `label` included, so a bound that covered only the chips can't be walked around by a copy edit. Scoring **never gates the session**: `canContinue`, `micAction` and `advanceDelayMs` are all independent of it, the card mounts in a worded pending state the instant the request leaves, and a failure invents no number, never blames the mic and never says "tải lại trang". `MAX_CLIP_MS` (30 s) is the cost guard — a longer take is **not sent and not truncated** (a verdict on the first 30 s of a 90 s take is a verdict on a different recording), and that ordering is a value, not an event handler: `scoringPlan(durationMs, disclosed)` returns `skip` (write `skipScoring`, nothing sent) or `send`, and its skip branch hands the flag back **untouched** so a first take that is too long cannot burn the once-per-session privacy notice and leave the real first upload silent. `useTurnScorer` supplies the flag to `scoringPlan` without committing it and claims the disclosure inside `score()`, once the blob is confirmed present — a take whose blob has vanished must not spend it either — emitting it through the `onDisclose` callback in the instant before the upload, never in the resolved value (that arrives seconds later, and telling someone their voice *was* uploaded is not a disclosure). The over-30 s message has exactly **one** surface: `scoreCard`'s `too-long` detail already is `CLIP_TOO_LONG_NOTICE.text`, so the plan's own `notice` is deliberately not shown — the card is anchored to the turn and does not vanish with the next notice. A 413 (the route's byte cap firing where the millisecond cap did not) lands on the same `skipScoring`. The hook's other honest distinctions: a 200 carrying `RecognitionStatus: NoMatch` / `InitialSilenceTimeout` / `BabbleTimeout` means Azure worked and heard no speech — it gets `NO_SPEECH_NOTICE` and never the "service returned something unreadable" wording, and it never blames the mic (`lib/recording.test.ts`'s rule, and just as true here); a 200 whose body is truncated or not JSON is `unreadable`, **not** `network` ("có vẻ mất mạng" over our own broken payload sends the learner to check their wifi); and a missing or non-finite `latencyMs` falls back to the round trip measured around the `fetch` rather than rendering `0` as "0.0s", a measured time that was never measured. `ScoreCard` keeps the two seconds apart — `latencySeconds` (scoring) and `clipSeconds` (the take, only on `too-long`, and rendered with a label) — because one unlabelled field once rendered a 41 s recording where every other card renders its latency. The score slot lives in `SessionState.scores` keyed by turn index, is dropped alongside the take in `startRecording`, and is written by the single `withScore` writer so `scores` and `CompletedTurn.score` cannot drift — an assessment routinely lands *after* "Tiếp", and a snapshot would freeze the completed turn on `pending` for ever. Unlike takes, scores survive `endSession` (no blob to collect, and Story 2.7 reads them) but are still **never persisted**. Two rules the card's markup carries: it is **not** its own live region — it sits inside the transcript's `role="log" aria-live="polite"`, so the subtree is muted with `aria-live="off"` and one short `role="status"` summary speaks instead, or the whole card (verdict, chips, both finding lists) is re-announced on `pending → scored` and again every time the `<details>` opens; and the glyph/IPA/number of a chip are `aria-hidden` decoration, with `.session-score__sr` carrying the same three facts as words, because `cold /d/ 10` read aloud says nothing about pass or fail. The three tints (`--score-pass/fail/warn`) are declared per theme on `.session-screen`, beside `--glow-record*` and for the same reason — a tint declared once, in the dark values only, pairs a dark wash with a light border the moment the theme flips.
- Hint ladder, opened (Story 2.4): the two rungs generated with the script since Story 1.3 (`DialogueHints`, `turnHasHints`) finally have a control — `HintDepth = "none" | "situation" | "keywords"`, `hintDepth`/`hintAvailable`/`openHint` in `lib/session.ts`, wired to the control row's hint slot in `SessionView.tsx`, with `.session-hint-chip` in `globals.css` rendering both rungs. **Not a fifth leak-prone projection** the way `scoreCard` was a fourth one: `SessionState.hints` carries only *how far* a turn's ladder was opened, never the ladder's text, so `TurnView` is untouched and the existing leak sweeps (which already include `"Khi bạn chào lại"` in their secrets list) keep passing unmodified — the content was always reachable at `script.turns[i].hints`, only the UI was gated off. `hintAvailable`/`openHint` are implicit-cursor, exactly like `micAction`/`startRecording`: they read `state.cursor`, never take a turn index, so the button can only ever act on the turn on screen. Fixed order, no skipping (`none → situation → keywords`), and `openHint` no-ops once a turn is at `"keywords"` or has no usable ladder (`turnHasHints` — a v2 script, or a learner turn Gemini generated without hints) — that is the third tap doing nothing and the slot's permanent-disabled state, not two different code paths. **Keyed like `takes`/`scores` but with the opposite reset rule**: `startRecording` drops that turn's take and score because a fresh attempt outdates both, but never touches `hints` — a hint once opened stays open for the rest of the turn's life, re-recorded or not, and past `endSession` too (nothing here is persisted either; a reload still discards it, same as the rest of `SessionState`). Opening a hint has zero coupling to `lib/pronunciation.ts` or the scoring transitions — `canContinue`, `micAction` and `advanceDelayMs` are all independent of it, same as scoring already is.
- Binary storage: `lib/blob-store.ts` — IndexedDB (`ankichat_blobs`), the app's *second* persistence layer, namespaced (`putBlob`/`getBlob`/`hasBlob`/`deleteBlob`/`deleteNamespace`, keys are `blobId(namespace, key)` = `${namespace}:${key}` — every operation goes through it, and `blob-store.test.ts` pins that they agree, because dropping the namespace from one side alone makes `hasBlob` false forever and silently re-bills Azure on every reopen). Story 1.4 owns the `"tts"` namespace and Story 2.2 owns `"recordings"` (`RECORDING_NAMESPACE` in `lib/recording.ts`); keep the layer general — don't fold either story's specifics into it. The two namespaces have **opposite lifetimes**: `"tts"` is paid for once and kept forever, `"recordings"` is scratch space for exactly one session, dropped at session start *and* at session end (`clearTakes`, fire-and-forget; the end is watched via `session.status === "finished"` so a session that simply runs out of turns is cleaned up too, not just one the user ends). Clearing takes must never touch `"tts"` — `deleteNamespace` cursors `by_namespace`, so handing over `namespacedBlobStore(RECORDING_NAMESPACE)` makes that true by construction rather than by care. A take's key is **positional** (`recordingKey(entryId, turnIndex)`), not a content hash: two takes are never identical and must never share a blob, re-recording overwrites in place, and no attempt counter is kept (that is Story 2.5's). Audio never goes in `localStorage`: `lib/history.ts` stays the text layer, and `historyStorage.clear()` drops the whole `"tts"` namespace with it (fire-and-forget) so blobs don't outlive every entry they belonged to. Cleanup is deliberately all-or-nothing: blobs are content-keyed and **shared** between entries, so deleting one entry's keys can mute another entry's lines — don't add per-entry cleanup without solving that first. `vitest` runs in `environment: "node"` and there is **no IndexedDB in tests**, which is why `lib/sample-audio.ts` is IndexedDB-free and takes a `SampleAudioStore` parameter — the claims worth testing (a reopened script fetches nothing, content-key identity and voice separation, quota handling) live there and are covered; `blob-store.ts` itself is the thin hand-verified wrapper. Don't add `fake-indexeddb` to move that line.
- History/persistence: `lib/history.ts` — the only persistence layer in the app. `HistoryEntry.script` is the structured script, v2 or v3 alike; entries written before Story 1.2 stored one markdown string and normalize on read to `script: null` + `legacyDialogue`. Consumers branch on `script` and fall back to `legacyDialogue`. In practice a legacy entry only ever reaches `VoicePractice.tsx` (via `/practice?id=…`); `DialogueDisplay.tsx` also keeps a legacy markdown branch, but nothing currently routes an old entry into it — it is retained-but-unreachable, so don't delete it and don't assume it is exercised either. `getAll()` skips stored elements that aren't presentable as entries and strips malformed `hints` via `withoutMalformedHints` before the shape guard runs — a garbled ladder costs the entry its hints, never its script; `readRaw` stays raw so mutators never rewrite what is already in `localStorage`.

## Running and verifying

- Tests run with Vitest: `npm test` (`vitest run`, config in `vitest.config.mts` — the `.mts` extension is deliberate, `.ts` triggers a Vite `configLoader` deprecation warning). Specs are `*.test.ts` next to the code, under `lib/`, `app/`, `components/`, or `hooks/`. Coverage is thin and node-environment only — there is no jsdom, so no component or E2E testing setup; a `.test.tsx` will be collected and then fail for want of a DOM.
- `npm run lint` is **not** clean and isn't expected to be: 3 pre-existing `react-hooks/set-state-in-effect` errors at `app/page.tsx:15`, `app/practice/page.tsx:18`, `components/HistoryPanel.tsx:19`, plus assorted unused-var warnings. They predate the vocabulary-source refactor — don't go chasing them as if you broke something. `npx tsc --noEmit` and `npm run build` are clean, and should stay that way.
- Anki Desktop + the AnkiConnect add-on must be running locally on port 8765 for anything past the landing page to work; there's no way to tell this from the code alone.
- `GEMINI_API_KEY` must be set in `.env.local`; the literal placeholder `"your_gemini_api_key_here"` is treated as "not configured" by app code.
- `AZURE_SPEECH_KEY` / `AZURE_SPEECH_REGION` must be set in `.env.local` for sample audio (`/api/tts`) and pronunciation assessment (`/api/pronunciation`) — one key, one bill, two endpoints. Missing either one is a 503 carrying the same Vietnamese message from `/api/tts` and from the pronunciation route's **azure branch** (that route also has a gemini branch with its own message); a key Azure *rejects* is a 401 from **both** routes. The client treats both as whole-script conditions and stops generating after one. The script still renders, it just has no audio; a session still runs, it just has no scores.

## Conventions that differ from defaults

- Styling is inline `style={{}}` plus global utility classes in `app/globals.css` — `tailwindcss` is a dependency but unused in components; don't introduce Tailwind classes.
- `app/globals.css` carries **two** colour layers, additively. The original 18 `--bg-*/--accent-*/--text-*` variables still paint the nine older files and are read-only. Beside them sits the DESIGN.md token layer: 11 semantic colours (`--surface-base/raised/sunken`, `--ink-primary/secondary/muted`, `--chrome`, `--success`, `--danger`, `--warning`, `--border-hairline`) plus `--font-sans`, `--font-phonetic`, `--space-1..6` (the 4px scale — nothing off it) and `--radius-sm/md/lg/full`. `--font-phonetic` has exactly one consumer, `.session-score__ipa` — Inter's Google-Fonts subset is latin + latin-ext, so IPA (θ ð ʃ ɪ ə) falls back, and the token makes that fallback deliberate; use it for every IPA symbol and nothing else. Dark values live on `:root`; only the 11 colours are redefined under `:root[data-theme="light"]`, and `app/layout.tsx` hardcodes `data-theme="dark"`. `color-scheme` is deliberately **not** on `:root` — there it is app-wide and repaints native scrollbars and form controls (the `<select>` in `DeckSelector`, the range input in `DialogueGenerator`, the checkbox in the pronunciation lab) on all nine legacy screens; it is scoped to `.session-screen` instead, and any future screen that wants it must opt in the same way. There is deliberately **no** `prefers-color-scheme` — DESIGN.md rejects following the OS — and no toggle yet. New screens use the tokens; retiring the purple `#7c3aed` / blue `#3b82f6` palette across the nine files that use it is separate deferred work, so don't repaint them as a side effect. Layer with background colour, not shadow (the mic's recording glow is the one exception), and never convey a state by colour alone — every status needs a glyph plus words a screen reader can reach.
- UI copy is in Vietnamese — keep new user-facing strings consistent with that.
- `app/api/anki/route.ts` enforces a hardcoded `ALLOWED_ACTIONS` whitelist (read-only) on AnkiConnect calls — extend it deliberately, never remove or bypass the check.
- `app/api/dialogue/route.ts` falls through a hardcoded list of Gemini model names on failure/quota errors — when changing models, edit the `models` array there, not the `ai`/`@ai-sdk/google` SDK deps (they're installed but unused; the route calls the Gemini REST endpoint directly). `thinkingConfig: { thinkingBudget: 128 }` is deliberate — `0` is rejected with a 400 by some 3.x models, and omitting it lets hidden reasoning eat `maxOutputTokens`.

## Known pitfalls

- `app/api/live-token/route.ts` returns the raw `GEMINI_API_KEY` to the browser — deliberate dev-only shortcut, not a bug to "fix"; production would need ephemeral tokens instead. It is **not** a pattern to copy: `app/api/tts/route.ts` and `app/api/pronunciation/route.ts` both proxy Azure precisely so `AZURE_SPEECH_KEY` never reaches the client. The two are deliberately the same shape and should stay that way — a bounded server timeout (15 s) that answers **504**, a 401/403 branch that answers 401 with the credential message (a rejected key is not a transport fault and no retry fixes it), key redaction on *every* error path including the thrown-message one, and a hard cap on the field that is billed (`MAX_TEXT_LENGTH` for TTS characters, `MAX_AUDIO_BYTES` for assessment seconds — 1.2 MB, just above 30 s of 16 kHz/16-bit mono, consistent with `MAX_CLIP_MS`). The client sets a *looser* timeout than the server (20 s vs 15 s) so the server's specific message wins the race. Any new provider route proxies and copies these four.
- `/api/tts` builds SSML, which is XML — model text goes through the route's own XML escape, **not** `escapeHtml` from `lib/dialogue/words.ts` (that one leaves `"` and `'` alone, which is fine for a text node and wrong near an attribute). It also strips what XML cannot carry at all — control characters, lone surrogates, U+FFFE/U+FFFF — since no entity makes those legal and a lone surrogate breaks the request's own UTF-8. The voice name is checked against `ALLOWED_VOICES` rather than escaped, the same closed-set reflex as `ALLOWED_ACTIONS` in the Anki route; `ALLOWED_VOICES` is written out **independently** of `SPEAKER_VOICES` (not derived from it) so the test that they agree can actually fail — same two-declarations-one-test shape as `DIALOGUE_SCRIPT_VERSION` / `SUPPORTED_SCRIPT_VERSIONS`. `text` is length-capped too: it is the field Azure bills per character. Both sides of the proxy set a timeout (server 15s, client 20s so the server's message wins), because a hung connection would otherwise stall the sequential loop forever with no error and no notice.
- If AnkiConnect isn't running, deck loading fails with a Vietnamese connection-error message that can look like a UI bug rather than a missing dependency.
- `color-mix()` is **not** safe in `app/globals.css`: lightningcss (via Turbopack) emits a browser-target fallback for it outside `@keyframes`, and for `color-mix(in srgb, var(--token) 45%, transparent)` that fallback is the bare `var(--token)` — the alpha is silently dropped and a soft glow renders as a hard opaque ring. Inside `@keyframes` it is left alone, so the *same* declaration behaves differently in two places and only one of them looks wrong. Declare literal `rgba()` values in a screen-scoped block instead (see `--glow-record*` on `.session-screen`).
- Turbopack's cache in `.next` can serve a **stale** `globals.css` after an edit: new custom properties resolve to `""` in the browser while `npm run build` output has them, so a screen renders with every `var()` falling through and looks merely "wrong" rather than broken. `rm -rf .next` and restart `npm run dev` before concluding that a CSS change did not take.

<!-- /bmad:context -->
